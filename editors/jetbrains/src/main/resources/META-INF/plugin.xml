<idea-plugin>
    <id>com.grahambrooks.forge</id>
    <name>Forge Preview</name>
    <vendor email="graham@grahambrooks.com" url="https://github.com/grahambrooks">Graham Brooks</vendor>

    <description><![CDATA[
        <p>Live diagram preview for <a href="https://github.com/grahambrooks/forge">Forge</a>
        <code>.forge</code> models.</p>
        <p>Open a <code>.forge</code> file and the rendered diagram appears beside the source. Every view
        the model defines &mdash; system context, containers, pipelines, deployment, data model, trust
        boundaries, teams, animated walkthroughs &mdash; can be picked from the preview toolbar.</p>
        <ul>
            <li>Updates as you type, including unsaved changes</li>
            <li>Re-renders when an <code>!include</code>d file changes</li>
            <li>Zoom, fit-to-width, and a palette that follows the IDE theme</li>
            <li>Model errors reported above the last good diagram instead of blanking it</li>
        </ul>
        <p>Rendering is done by the <code>forge</code> CLI, which must be installed (2026.9.8 or
        later to preview unsaved changes)
        (<code>brew install grahambrooks/forge/forge</code> or <code>cargo install</code>). Its location
        can be set under <b>Settings | Tools | Forge Preview</b>.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <!-- JCEF is part of the core platform up to 2025.x and a separate plugin from 2026.2. The
         optional dependency links in that plugin's classes where it exists; on older builds it is
         absent and JCEF comes from the core. -->
    <depends optional="true" config-file="forge-jcef.xml">com.intellij.modules.jcef</depends>

    <extensions defaultExtensionNs="com.intellij">
        <fileType name="Forge" implementationClass="com.grahambrooks.forge.ForgeFileType" fieldName="INSTANCE"
                  extensions="forge"/>
        <fileEditorProvider implementation="com.grahambrooks.forge.preview.ForgePreviewEditorProvider"/>
        <applicationConfigurable parentId="tools" id="com.grahambrooks.forge.settings"
                                 instance="com.grahambrooks.forge.settings.ForgeSettingsConfigurable"
                                 displayName="Forge Preview"/>
    </extensions>
</idea-plugin>
