[package]
name = "forge"
version = "0.1.0"
edition = "2021"
description = "A unified software modeling DSL — structure + process diagrams from code"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_yml = "0.0.12"
walkdir = "2"
