{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "Formula/forge.rb is generated by the release workflow (scripts/render-formula.py)",
    "and points each platform at a release *binary* tarball. Renovate's homebrew",
    "manager rewrites those URLs to source archives (archive/refs/tags/...), which",
    "contain no compiled binary and break `bin.install \"forge\"` - see #28 and #37.",
    "The release workflow is the single owner of that file."
  ],
  "homebrew": {
    "enabled": false
  }
}
